public class VigenereDecryptionUseCase implements DecryptionBoundary {
    @Override
    public String decrypt(String ciphertext, String key) {
        // Implementation of the decryption logic (same as before)
        return null;
    }
}
