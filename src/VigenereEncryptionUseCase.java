public class VigenereEncryptionUseCase implements EncryptionBoundary {
    @Override
    public String encrypt(String plaintext, String key) {
        // Implementation of the encryption logic (same as before)
        return null;
    }
}
